# Development environment variables for Docker
NODE_ENV=development
DATABASE_URL=postgresql://postgres_proposal:postgres_proposal@database:5432/proposal_db?schema=public
PORT=3000
WEB_PORT=3001

# API Configuration
API_PORT=3000
JWT_SECRET=your-jwt-secret-key-here
JWT_EXPIRES_IN=7d

# Database Configuration
DB_HOST=database
DB_PORT=5432
DB_USERNAME=postgres_proposal
DB_PASSWORD=postgres_proposal
DB_NAME=proposal_db

# Email Configuration (for development)
SMTP_HOST=localhost
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASS=your-email-password

# File Upload Configuration
UPLOAD_MAX_SIZE=10485760
UPLOAD_ALLOWED_TYPES=image/jpeg,image/png,application/pdf

# CORS Configuration
CORS_ORIGIN=http://localhost:3001

# Email Configuration
SENDGRID_API_KEY=your-sendgrid-api-key-here
FROM_EMAIL=noreply@proposal.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
